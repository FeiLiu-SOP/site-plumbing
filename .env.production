# 生产构建读取；Cloudflare Build 变量同名时优先覆盖本文件。
# 独立 Pages：请保持 SITE 为「本项目的 *.pages.dev」，以便 hub-site-path 得到 base=/，
# 州页/详情页链接与 dist 目录一致（勿在独立仓沿用裸主域 + 错误 ACTIVE_COLLECTION，否则会错成 /roofing/）。
PUBLIC_SITE_URL=https://site-plumbing.pages.dev
